name: Coverage

on:
  pull_request:
    branches: [ '**' ]
    paths:
      - 'camposer/**'
      - 'camposer-code-scanner/**'
      - 'camposer-permissions/**'
      - '.github/workflows/coverage.yml'
      - 'gradle/**'
      - 'gradle.properties'
      - '*.gradle.kts'
      - 'scripts/parse_coverage.py'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  coverage:
    runs-on: ubuntu-latest
    permissions:
      # deleteComment goes through the issues API even for PR conversation comments
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6.0.1
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v5.1.0
        with:
          distribution: zulu
          java-version: 21

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - uses: gradle/actions/setup-gradle@v4

      # ── After (PR branch) ────────────────────────────────────────────────────

      - name: Coverage — PR branch
        # testAndroidHostTest ignoreFailures=true in build — Android SDK stubs cause
        # ~half the tests to fail on JVM; real bugs are caught by connectedAndroidTest.
        run: ./gradlew koverXmlReport
        continue-on-error: true

      - name: Parse PR coverage
        id: after
        run: |
          REPORT="build/reports/kover/report.xml"
          if [ -f "$REPORT" ]; then
            pct=$(python3 scripts/parse_coverage.py "$REPORT" 2>/dev/null || echo "N/A")
          else
            pct="N/A"
          fi
          echo "pct=$pct" >> "$GITHUB_OUTPUT"

      # ── Before (base branch) ─────────────────────────────────────────────────

      - name: Checkout base branch
        env:
          BASE_REF: ${{ github.base_ref }}
        run: git checkout "origin/$BASE_REF"

      - name: Make gradlew executable (base)
        run: chmod +x ./gradlew

      # Both runs write the same path; a stale PR report would be read as the base.
      - name: Discard PR coverage report
        run: rm -f build/reports/kover/report.xml

      - name: Coverage — base branch
        run: ./gradlew koverXmlReport
        continue-on-error: true

      - name: Parse base coverage
        id: before
        run: |
          pct=$(python3 - << 'EOF'
          import xml.etree.ElementTree as ET
          try:
              root = ET.parse('build/reports/kover/report.xml').getroot()
              covered = missed = 0
              for c in root.findall('counter'):
                  if c.get('type') == 'LINE':
                      covered += int(c.get('covered', 0))
                      missed += int(c.get('missed', 0))
              total = covered + missed
              print(round(covered / total * 100, 1) if total else 0.0)
          except Exception:
              print('N/A')
          EOF
          )
          echo "pct=$pct" >> "$GITHUB_OUTPUT"

      # ── PR comment ───────────────────────────────────────────────────────────

      - name: Post coverage comment
        uses: actions/github-script@v7
        env:
          BASE_REF: ${{ github.base_ref }}
          PCT_BEFORE: ${{ steps.before.outputs.pct }}
          PCT_AFTER: ${{ steps.after.outputs.pct }}
        with:
          script: |
            const before = process.env.PCT_BEFORE;
            const after  = process.env.PCT_AFTER;
            const bNum = parseFloat(before);
            const aNum = parseFloat(after);

            let diffStr = 'N/A';
            let emoji   = '';
            if (!isNaN(bNum) && !isNaN(aNum)) {
              const diff = aNum - bNum;
              const sign = diff >= 0 ? '+' : '';
              diffStr = `${sign}${diff.toFixed(1)}%`;
              emoji   = diff > 0 ? ' 🟢' : diff < 0 ? ' 🔴' : ' ⚪';
            }

            const fmt = v => v === 'N/A' ? 'N/A' : `${v}%`;
            const body = [
              '## 📊 Coverage',
              '',
              '| Branch | Line coverage |',
              '|--------|--------------|',
              `| \`${process.env.BASE_REF}\` (before) | ${fmt(before)} |`,
              `| This PR (after) | ${fmt(after)} |`,
              `| **Diff** | **${diffStr}**${emoji} |`,
              '',
              '> Only JVM-reachable code is counted. CameraX / AVFoundation hardware code is excluded.',
            ].join('\n');

            const comments = await github.paginate(github.rest.issues.listComments, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const stale = comments.filter(
              c => c.user.login === 'github-actions[bot]' && c.body.startsWith('## 📊 Coverage')
            );
            for (const comment of stale) {
              await github.rest.issues.deleteComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: comment.id,
              });
            }
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });
